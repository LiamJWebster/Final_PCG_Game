using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testingbruh : MonoBehaviour
{
    
    public static int JustATest()
    {
        return 1;
    }

    public static int testingDude = JustATest();


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
